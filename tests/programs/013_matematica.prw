// Valida a precedencia de operadores matematicos.
User Function TesteMatematica()

    ConOut(Str(2 + 3 * 4))
    ConOut(Str((2 + 3) * 4))
    ConOut(Str(2 ^ 3 * 2))
    ConOut(Str(10 - 2 - 3))
    ConOut(Str(-2 ^ 2))
    ConOut(Str(2 + 2 ^ 3))

Return
